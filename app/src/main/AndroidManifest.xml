<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Legacy storage permissions for Android 9 and below -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Broad file access so notes can live in the shared Android/media/notes folder -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- App lock (fingerprint/face/PIN via the system's own screen lock) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".LocalNotesApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalNotes">

        <activity
            android:name=".ui.notes.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.LocalNotes.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.editor.NoteEditorActivity"
            android:exported="false"
            android:parentActivityName=".ui.notes.MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.calculator.CalculatorActivity"
            android:exported="false"
            android:parentActivityName=".ui.notes.MainActivity" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:parentActivityName=".ui.notes.MainActivity" />

        <activity
            android:name=".ui.security.LockActivity"
            android:exported="false"
            android:theme="@style/Theme.LocalNotes.Splash" />

        <activity
            android:name=".ui.security.PinSetupActivity"
            android:exported="false"
            android:theme="@style/Theme.LocalNotes.Splash" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
